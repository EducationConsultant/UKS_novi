sudo: required
language: python
python:
  - "3.5"
#services: docker

# command to install dependencies
install:
  - pip install -r requirements.txt
  
  
# command to run tests
script:
  - python tests.py
  
  
branches:
  only:
- master
